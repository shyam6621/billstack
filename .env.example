# Frontend environment variables (Vite)
# Copy to .env.local for local overrides

# Backend API base URL — must include /api suffix
# Local dev: http://localhost:8080/api
# Production: https://your-render-app.onrender.com/api
VITE_API_URL=http://localhost:8080/api

# --- Optional: use Railway MySQL during local backend development ---
# When set, override the default file-based H2 database in application-dev.yml
# SPRING_DATASOURCE_URL=jdbc:mysql://HOST:PORT/railway?useSSL=true&serverTimezone=UTC
# SPRING_DATASOURCE_USERNAME=root
# SPRING_DATASOURCE_PASSWORD=your-password
# SPRING_DATASOURCE_DRIVER=com.mysql.cj.jdbc.Driver
# SPRING_JPA_DIALECT=org.hibernate.dialect.MySQLDialect
